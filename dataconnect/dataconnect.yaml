specVersion: "v1beta"
serviceId: "Customer Data handle"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Customer Data handle-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
